syntax = "proto3";//版本
option  java_outer_classname = "StudentPOJO";//生成的外部类名，同事也是文件名
//protobuf使用message管理数据
message Student{//会在StudentPOJ0外部类生成一个内部类Student,他是真正发送PoJ0对象
  int32 id = 1;//Student类中有一个属性名字为id类型为int32(protobuf类型1表示属性序号，不是值
  string name = 2;
}